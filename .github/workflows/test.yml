name: check test
on:
  - pull_request
jobs:
  sample:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: ['dev', 'stg', 'prd']
    steps:
      - uses: actions/checkout@v2
      - run: HOGE=$(echo "hoge") echo $HOGE${{ matrix.env }}
